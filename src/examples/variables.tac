main = (argc: int, argv: Array<string>) => {
    var: string = "hello this string is even longer than the last\n";
    varone: string = "this ist the string value of variable 'var1'\n";
    print(var);
    print(varone);
    return 0;
};